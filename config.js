/*
  Optional Supabase connection. Paste the values from:
  Supabase > Project Settings > API
  Leave both values empty to run this app locally on the device.
*/
window.UNIPOP_STOCK_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: ""
};
